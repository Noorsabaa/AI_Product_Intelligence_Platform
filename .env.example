# Optional shell environment settings; this file is documentation, not loaded automatically.
# INTELLIGENCE_DB=data/reviews.db
# TOPIC_MODEL=sentence-transformers/all-MiniLM-L6-v2
# SENTIMENT_MODEL=cardiffnlp/twitter-roberta-base-sentiment-latest
# Category discovery and text sentiment currently support English.

# Enable only when serving through HTTPS:
# COOKIE_SECURE=1
# Optional account directory override:
# ACCOUNTS_DB=data/accounts.db
