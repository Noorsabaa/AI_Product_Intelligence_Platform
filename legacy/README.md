# Archived implementation

These files are preserved from the supplied project for reference. They are not imported by Signal and are not supported entry points. Some contain old hardcoded paths, obsolete dependency assumptions and references to the old CSV-derived pipeline.

Use `start.ps1` and `scripts/manage.py` in the project root. The canonical pipeline is in `services/`. The original README and project history are retained here to explain earlier experiments; they are historical documents, not setup instructions for the rebuilt application.
